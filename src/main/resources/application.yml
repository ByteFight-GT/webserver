spring:
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  servlet:
    multipart:
      max-file-size: 1GB
      max-request-size: 1GB
app:
  auth:
    # These are dummy secret values, only for local development
    jwt-secret: excyS1grB5eLRbxWl4oomyasIXgBeYlSicEBLOmAtzs
    admin-secret: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJyb2xlIjoic2VydmljZV9yb2xlIiwiaXNzIjoic3VwYWJhc2UiLCJhdWQiOiJhdXRoZW50aWNhdGVkIiwiaWF0IjowLCJleHAiOjQwNzA5MzA0MDB9.9YbsIjpvXGnOO3Mrs_V-U3iMiJnLum98P0TLpSgzgek
    base-url: http://localhost:9999
    read-timeout-ms: 5000
    connect-timeout-ms: 3000
springdoc:
  writer-with-order-by-keys: true
  swagger-ui:
    tags-sorter: alpha
gamematch:
  requeue-stale: true
  stale-threshold-minutes: 120
matchmaking:
  # max-matches-per-event bounds a single matchmaking tick. Unset = unlimited (historical
  # behaviour). Set it to what the runner fleet can clear before the next tick to keep the queue
  # from growing monotonically under automatic load.
  max-matches-per-event:
storage:
  max-decompressed-bytes: 524288000

turnstile:
  enabled: ${TURNSTILE_ENABLED:true}
  secret-key: ${TURNSTILE_SECRET_KEY:}